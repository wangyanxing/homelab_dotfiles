# Machine-local overrides loaded BEFORE the main config.
# Put a *.zsh file here for anything you want set early
# (e.g. custom PATH, env vars, proxy). Not committed to shared config.
#
# Example: create ~/.zsh.before/00-local.zsh
#   export SOME_TOKEN=...
