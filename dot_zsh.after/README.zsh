# Machine-local overrides loaded AFTER the main config.
# This is where you override aliases, add machine-specific functions, etc.
# Loaded last, so it wins over everything in ~/.config/zsh/.
#
# Example: create ~/.zsh.after/00-local.zsh
#   alias deploy='...'
